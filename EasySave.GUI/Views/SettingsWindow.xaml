<Window x:Class="EasySave.GUI.Views.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="{Binding [SettingsTitle], Source={StaticResource Lang}}"
        Height="560" Width="520"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">
    <Window.Resources>
        <Style TargetType="Label">
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Margin" Value="0,10,0,3"/>
        </Style>
        <Style TargetType="TextBox">
            <Setter Property="Padding" Value="8"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Height" Value="32"/>
        </Style>
        <Style TargetType="ComboBox">
            <Setter Property="Padding" Value="8"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Height" Value="32"/>
        </Style>
        <Style TargetType="Button">
            <Setter Property="Padding" Value="15,8"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="MinWidth" Value="80"/>
            <Setter Property="Cursor" Value="Hand"/>
        </Style>
    </Window.Resources>
    <Grid Margin="20,15">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <ScrollViewer Grid.Row="0" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
            <StackPanel>
                <Label Content="{Binding [LogFormat], Source={StaticResource Lang}}"/>
                <ComboBox ItemsSource="{Binding LogFormatValues}" SelectedItem="{Binding LogFormat}" Margin="0,0,0,8"/>

                <Label Content="{Binding [LogDestination], Source={StaticResource Lang}}"/>
                <ComboBox ItemsSource="{Binding LogDestinationValues}"
                          SelectedItem="{Binding LogDestination}"
                          Margin="0,0,0,8"/>

                <Label Content="{Binding [LogServerUrl], Source={StaticResource Lang}}"/>
                <TextBox Text="{Binding LogServerUrl, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,8"/>

                <Label Content="{Binding [PriorityExtensions], Source={StaticResource Lang}}"/>
                <Grid Margin="0,0,0,4">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <ComboBox Grid.Column="0" ItemsSource="{Binding AvailableExtensions}"
                              SelectedItem="{Binding SelectedPriorityExtension}"
                              Margin="0,0,8,0"/>
                    <Button Grid.Column="1" Content="{Binding [Add], Source={StaticResource Lang}}" Command="{Binding AddPriorityExtensionCommand}" Background="#0078D4" Foreground="White"/>
                </Grid>
                <ListBox ItemsSource="{Binding PriorityExtensionsList}"
                         SelectedItem="{Binding SelectedPriorityToRemove}"
                         Height="70" Margin="0,0,0,4"/>
                <Button Content="{Binding [RemoveSelected], Source={StaticResource Lang}}" Command="{Binding RemovePriorityExtensionCommand}" Margin="0,0,0,12"/>

                <Label Content="{Binding [EncryptExtensions], Source={StaticResource Lang}}"/>
                <Grid Margin="0,0,0,4">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <ComboBox Grid.Column="0" ItemsSource="{Binding AvailableExtensions}"
                              SelectedItem="{Binding SelectedEncryptExtension}"
                              Margin="0,0,8,0"/>
                    <Button Grid.Column="1" Content="{Binding [Add], Source={StaticResource Lang}}" Command="{Binding AddEncryptExtensionCommand}" Background="#0078D4" Foreground="White"/>
                </Grid>
                <ListBox ItemsSource="{Binding ExtensionsToEncryptList}"
                         SelectedItem="{Binding SelectedEncryptToRemove}"
                         Height="70" Margin="0,0,0,4"/>
                <Button Content="{Binding [RemoveSelected], Source={StaticResource Lang}}" Command="{Binding RemoveEncryptExtensionCommand}" Margin="0,0,0,12"/>

                <Label Content="{Binding [LargeFileThreshold], Source={StaticResource Lang}}"/>
                <TextBox Text="{Binding LargeFileThresholdKoText, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,8"/>

                <Label Content="{Binding [BusinessSoftware], Source={StaticResource Lang}}"/>
                <TextBox Text="{Binding BusinessSoftwareName, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,4"/>
                <TextBlock Text="{Binding [BusinessSoftwareHint], Source={StaticResource Lang}}"
                           FontSize="11" Foreground="#666666" Margin="0,0,0,15" TextWrapping="Wrap"/>
            </StackPanel>
        </ScrollViewer>

        <Border Grid.Row="1" Background="#F5F5F5" Padding="10,12" BorderBrush="#E0E0E0" BorderThickness="0,1,0,0">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="{Binding [Apply], Source={StaticResource Lang}}" Command="{Binding ApplyCommand}" Background="#0078D4" Foreground="White"/>
                <Button Content="{Binding [Save], Source={StaticResource Lang}}" Command="{Binding SaveCommand}" Background="#107C10" Foreground="White"/>
                <Button Content="{Binding [Cancel], Source={StaticResource Lang}}" Command="{Binding CancelCommand}" Background="#6c757d" Foreground="White"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>

using System.Windows;

namespace EasySave.WPF
{
    /// <summary>
    /// Code-behind for MainWindow.
    /// Minimal logic here as MVVM pattern handles everything in ViewModels.
    /// </summary>
    public partial class MainWindow : Window
    {
        public MainWindow()
        {
            InitializeComponent();
        }
    }
}